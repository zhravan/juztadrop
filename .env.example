# Environment
NODE_ENV=development
# Options: development, production
# - development: Enables Swagger, continues on migration errors
# - production: Disables Swagger, exits on migration errors

# Secrets Management Backend
SECRETS_BACKEND=env
# Options: env, docker, infisical
# - env: Environment variables (default, for local dev)
# - docker: Docker secrets (for Docker Swarm)
# - infisical: Infisical (for team collaboration and production)

# Infisical Configuration (required if SECRETS_BACKEND=infisical)
# Get credentials from Infisical dashboard: https://app.infisical.com
# 1. Go to Settings → Service Tokens
# 2. Create a new service token
# 3. Copy the token and project ID
INFISICAL_TOKEN=your-service-token
INFISICAL_PROJECT_ID=your-project-id
INFISICAL_ENVIRONMENT=dev
# Options for INFISICAL_ENVIRONMENT: dev, staging, production

# Logging
LOG_LEVEL=info
# Options: trace, debug, info, warn, error, fatal
# Default: 'info' in production, 'debug' in development

# Database (individual parameters - preferred)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=justadrop

# Database URL (optional - will be constructed from above if not provided)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/justadrop

# Database Migrations
RUN_MIGRATIONS=true
# Set to 'false' to skip migrations on startup
# Useful when running migrations separately (e.g., init container, migration job)
# Default: true

# API Configuration
API_PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:3001

# Resend Email Configuration
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=noreply@justadrop.xyz
RESEND_FROM_NAME=Just a Drop
# Get API key from: https://resend.com/api-keys

# App Ports (for Docker Compose)
VIEW_PORT=3000
DASHBOARD_PORT=3002
